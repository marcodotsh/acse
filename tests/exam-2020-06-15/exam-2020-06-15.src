int x, y, z, a, b;

read(x);
read(y);
read(z);

a = 0;
b = 0;

iterate
{
    x = x-1;
    y = y+1;
    iterate {
        a = a+1;
        b = b-1;
    }
    times(1)
    unless(0);
}
times (z+5)
unless (x<=0);

write(x);
write(y);
write(z);
write(a);
write(b);

